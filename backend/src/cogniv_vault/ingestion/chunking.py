"""PDF chunking — ~500 token windows with 50 token overlap. Phase 2 stub."""


def chunk_text(text: str, window: int = 500, overlap: int = 50) -> list[str]:
    raise NotImplementedError("chunking is Phase 2")
