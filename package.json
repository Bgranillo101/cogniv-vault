{
  "name": "cogniv-vault",
  "version": "0.1.0",
  "private": true,
  "description": "Observable agentic RAG system with pixel-art visualization",
  "packageManager": "pnpm@10.0.0",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "pnpm --filter frontend dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck"
  },
  "license": "MIT"
}
